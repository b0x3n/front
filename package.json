{
  "name": "front",
  "version": "1.0.0",
  "description": "Test...",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./dist/bundle.js && webpack ./modules/Bootstrap.js --output-filename=bundle.js --mode=development"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.92.0",
    "webpack-cli": "^5.1.4"
  }
}
